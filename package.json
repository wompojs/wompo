{
	"name": "womp",
	"version": "1.0.0",
	"main": "index.js",
	"author": "LL",
	"license": "ISC",
	"description": "",
	"scripts": {
		"dev": "esbuild ts/womp.ts --sourcemap --watch --outdir=dist --allow-overwrite",
		"build": "esbuild ts/womp.ts --minify --outdir=dist --allow-overwrite",
		"jsx": "esbuild test/counter.tsx --outdir=test --allow-overwrite --watch",
		"perf": "tach --config tachometer.json"
	},
	"devDependencies": {
		"esbuild": "0.20.1",
		"ts-loader": "^9.4.3"
	},
	"dependencies": {
		"tachometer": "^0.7.0"
	}
}
